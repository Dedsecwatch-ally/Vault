apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-config
  namespace: vault
data:
  NODE_ENV: "production"
  PORT: "3000"
  STORAGE_PROVIDER: "local"
  UPLOAD_DIR: "/app/uploads"
  MAX_FILE_SIZE: "52428800"
  JWT_EXPIRES_IN: "7d"
